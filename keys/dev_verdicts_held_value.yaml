description: "Developer tools screen: formatted value readout for 'verdicts held'."
source: "{held} / {total}"
platforms:
  - android
placeholders:
  - name: held
    type: int
  - name: total
    type: int
localizations:
  ar: "{held} / {total}"
  de: "{held} / {total}"
  en: "{held} / {total}"
  es: "{held} / {total}"
  ru: "{held} / {total}"
  zh: "{held} / {total}"
