description: "Account tab: explanation under the post quantum identity panel — what the identity key is and how verifying provider keys detects interception."
source: "Your identity key is stored locally on this device. If any peer's key appears different than their locally stored key, it means the network operator cannot be trusted."
platforms:
  - site
  - linux
  - windows
  - android
  - apple
localizations:
  en: "Your identity key is stored locally on this device. If any peer's key appears different than their locally stored key, it means the network operator cannot be trusted."
