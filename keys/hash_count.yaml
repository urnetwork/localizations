description: "General UI string."
source: "#%lld"
platforms:
  - apple
placeholders:
  - name: count
    type: int
localizations:
  ar: "#{count}"
  de: "#{count}"
  en: "#{count}"
  es: "#{count}"
  ru: "#{count}"
  zh: "#{count}"
