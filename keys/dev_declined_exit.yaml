description: "Developer page: report line when the SDK refused an exit action."
source: "No change: the SDK declined that exit"
translatable: false
platforms:
  - linux
localizations:
  en: "No change: the SDK declined that exit"
