description: "IP versions: the tooltip of the address-family tag column in the provider locations sheet (windows, linux). It also titled the connect drawer's IP version histogram before that became the Dualstack / IPv4 / IPv6 status row."
source: "IP versions"
deprecated:
  - apple
platforms:
  - apple
  - linux
  - windows
localizations:
  en: "IP versions"
